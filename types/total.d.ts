export type Total = {
  total: number;
  setTotal: (num) => void;
  subTotal: number;
  setSubTotal: (num) => void;
  tax: number;
  setTax: (num) => void;
  amount: number[];
  setAmount: (num) => void;
};
